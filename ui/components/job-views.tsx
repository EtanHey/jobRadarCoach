"use client";
import type { DuplicateJobGroup } from "@/lib/job-dedup";
import type { ReactNode, RefObject } from "react";
import Link from "next/link";
import { Search } from "lucide-react";
import type { JobSummary } from "@/lib/contracts";
import { Button } from "@/components/ui/button";
import type { BoardFilter } from "@/lib/job-board-preferences";
import { JobCards } from "./job-cards";
import { ThemeToggle } from "./theme-toggle";
import { scoreBands, scoreCss } from "@/lib/globe-model";
export { JobDrawer } from "./job-drawer";
export type Filter = BoardFilter;
const filters: Record<Filter, string> = { all: "All roles", "new-for-me": "New for me", seen: "Seen" };
type Enriched = { source?: string; last_seen_at?: string; experience?: string | null; seniority_origin?: string; extraction_state?: string };
type ViewJob = JobSummary & Enriched;
type Opener = RefObject<HTMLButtonElement | null>;
type RowProps = { job: ViewJob; alternateCount?: number; openerRef: Opener; selectJob: (id: string | null) => void };
type ListProps = { visiblePostingIds?: readonly string[]; globe?: ReactNode; globeMeta?: ReactNode; globeOpen?: boolean; selectedId?: string | null; openDetail?: (id: string) => void; filter: Filter; search: string; jobs: JobSummary[]; groups: DuplicateJobGroup[]; loading: boolean; error: string; openerRef: Opener; selectJob: RowProps["selectJob"]; chooseFilter: (filter: Filter) => void; setSearch: (value: string) => void; reload: () => void; toolbar?: ReactNode; resultLimit?: number; sortLabel?: string; loadedUpdatedAt?: string | null };

export function BoardHeader({children}: {children?: ReactNode}) { return <header className="border-b bg-background/90 backdrop-blur"><div className="flex w-full items-center justify-between gap-4 px-4 py-4 sm:px-6 lg:px-8"><Link href="/" className="flex items-center gap-3 font-semibold tracking-tight"><span aria-hidden="true" className="grid size-9 place-items-center rounded-xl bg-foreground/10 text-foreground">J</span>Job Radar <span className="hidden font-normal text-muted-foreground sm:inline">/ Coach</span></Link><div className="flex items-center gap-2">{children}<ThemeToggle /></div></div></header> }

function Placeholder({loading}: {loading: boolean}) { return <div className="grid min-h-[35rem] place-items-center px-6 py-16 text-center"><div>{loading ? <><div className="mx-auto size-8 animate-spin rounded-full border-2 border-muted border-t-primary" /><p className="mt-4 text-sm text-muted-foreground">Loading roles…</p></> : <><h2 className="text-xl font-medium">No roles in this view yet.</h2><p className="mt-2 text-sm text-muted-foreground">Try another filter or clear your search.</p></>}</div></div> }
export function JobsPanel({visiblePostingIds, globe, globeMeta, globeOpen = false, selectedId, openDetail, filter, search, jobs, groups, loading, error, openerRef, selectJob, chooseFilter, setSearch, reload, toolbar, resultLimit = 250, sortLabel}: ListProps) { return <section aria-label="Job search" className={globeOpen ? "job-search-globe" : ""}><div className="mb-2 flex flex-wrap items-center justify-between gap-2"><div className="flex max-w-full gap-1 overflow-x-auto" aria-label="Job filters">{(Object.keys(filters) as Filter[]).map((item) => <button key={item} aria-pressed={filter === item} onClick={() => item !== filter && chooseFilter(item)} className={`shrink-0 rounded-lg px-3 py-2 text-sm focus-visible:outline-2 focus-visible:outline-offset-2 ${filter === item ? "bg-foreground/10 text-foreground" : "text-muted-foreground hover:bg-muted"}`}>{filters[item]}</button>)}</div><label className="flex w-full items-center gap-2 rounded-lg border bg-card px-3 py-2 sm:w-80"><Search size={16} aria-hidden="true" /><span className="sr-only">Search loaded jobs by title, company, or stack</span><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search title, company, or stack" className="min-w-0 flex-1 bg-transparent text-sm outline-none" /></label></div>{toolbar}<div className="job-results-summary flex flex-wrap items-center gap-x-4 gap-y-2 pb-2 text-xs text-muted-foreground">{globeOpen && <span data-globe-legend className="flex flex-wrap items-center gap-x-3 gap-y-1">{scoreBands.map(band => <span key={band.label} className="flex items-center gap-1"><i aria-hidden="true" className="inline-block size-3 rounded-full" style={band.score === null ? { border: "2px solid var(--globe-unscored-ring)" } : { backgroundColor: scoreCss(band.score) }} />{band.label}</span>)}</span>}<span aria-live="polite">{loading ? "Loading roles…" : `${groups.length} ${groups.length === 1 ? "role" : "roles"}`}{jobs.length === resultLimit ? ` · latest ${resultLimit.toLocaleString()}` : ""}</span><span className="flex flex-wrap gap-x-2">{globeMeta}</span><span className="ml-auto">{sortLabel ?? (filter === "new-for-me" ? "Ranked for you" : "Newest first")}</span></div><div className={`globe-layout ${globeOpen ? "globe-layout-open" : ""}`}><div className="globe-slot">{globe}</div><div className="globe-rail">{error ? <div role="alert" className="min-h-[35rem] rounded-2xl border border-destructive/20 bg-card p-6 text-sm text-destructive">{error}<Button variant="outline" className="ml-3" onClick={reload}>Retry</Button></div> : (loading && jobs.length === 0) || groups.length === 0 ? <Placeholder loading={loading} /> : <JobCards {...{groups, globeOpen, visiblePostingIds, selectedId, openDetail, openerRef, selectJob}} />}</div></div></section> }
